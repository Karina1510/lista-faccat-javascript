let base = parseInt(prompt("Digite a base do retângulo: "))

let altura = parseInt(prompt("Digite a altura do retângulo: "))

let retangulo = base * altura

alert("o tamanho do seu retângulo é de " + retangulo)